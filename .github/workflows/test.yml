name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.21"

      - name: go vet
        run: go vet ./...

      - name: gofmt
        # Restricted to real source directories: testdata contains an
        # intentionally-invalid fixture (edge-cases/broken.go) that makes
        # gofmt exit non-zero, which would abort this step under bash -e.
        run: |
          fmt_out="$(gofmt -l cmd internal)"
          if [ -n "$fmt_out" ]; then
            echo "The following files are not gofmt'ed:"
            echo "$fmt_out"
            exit 1
          fi

      - name: go test
        run: go test ./...
